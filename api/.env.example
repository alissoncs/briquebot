# Postgres connection string (optional for the placeholder scaffold).
# The app boots and serves GET /health without it; TypeORM is only wired up
# once DATABASE_URL is set and real entities exist.
DATABASE_URL=postgresql://user:password@localhost:5432/briquebot

