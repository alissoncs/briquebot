# Neon (Vercel Postgres) connection string.
# Create a database at https://neon.tech (or via the Vercel Storage tab),
# then copy the pooled connection string here and into .env.local
DATABASE_URL="postgresql://<user>:<password>@<host>/<dbname>?sslmode=require"
